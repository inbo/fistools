% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/geom_pci.R
\name{geom_pci}
\alias{geom_pci}
\title{Potential for Conflict Index (PCI) Visualization Layer}
\usage{
geom_pci(
  mapping = NULL,
  data = NULL,
  position = "identity",
  na.rm = FALSE,
  show.legend = NA,
  inherit.aes = TRUE,
  bars = TRUE,
  bar_width = 0.4,
  max_height = 0.4,
  bar_fill = "gray88",
  pci_fun = qd_pci2,
  n_points = 60,
  ...
)
}
\arguments{
\item{mapping}{Set of aesthetic mappings created by \code{\link[ggplot2]{aes}}.}

\item{data}{The dataset to be displayed..}

\item{position}{Position adjustment (e.g., "identity").}

\item{na.rm}{Logical. If \code{TRUE}, missing values are silently removed.}

\item{show.legend}{Logical. Should this layer be included in the legends?}

\item{inherit.aes}{If \code{FALSE}, the default aesthetics are overridden.}

\item{bars}{Logical. If \code{TRUE} (default), the distribution bars are drawn in the background.}

\item{bar_width}{Numeric. The width of the background bars. Default is 0.4.}

\item{max_height}{Numeric. The maximum height proportion of the bars within their own row track. Default is 0.4.}

\item{bar_fill}{Character. The fill color of the background bars. Default is "gray88".}

\item{pci_fun}{Function. The algorithm used to compute the PCI index. Default is \code{qd_pci2}.}

\item{n_points}{Integer. The number of points used to smoothly generate the circle's polygon path. Default is 60.}

\item{...}{Other arguments passed directly to the underlying \code{\link[ggplot2]{layer}}.}
}
\value{
A \code{ggplot2} layer object (if \code{bars = FALSE}) or a list containing
two layers (background bars and foreground circles) if \code{bars = TRUE}.
}
\description{
This function constructs a composite ggplot2 layer that
automatically calculates and draws Potential for Conflict Index (PCI) circles
directly from raw ordinal data, optionally supported by structured background
micro-histograms.
}
\examples{
\dontrun{

# Example usage directly on raw data:
q6 <- readRDS("data/surveyMonkey_q6.rda")
# Without histogram in the background
q6 \%>\%
  filter(!is.na(score)) \%>\%
  ggplot(aes(x = score, y = fct_reorder(topic, score, mean))) +
  geom_pci(alpha = 0.8, bars = FALSE)

# With histogram in the background
q6 \%>\%
  filter(!is.na(score)) \%>\%
  ggplot(aes(x = score, y = fct_reorder(topic, score, mean))) +
  geom_pci(alpha = 0.8, bars = TRUE)
}

}
\author{
Martijn Bollen
}
