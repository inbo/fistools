% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/get_annotation_completeness.R
\name{get_annotation_completeness}
\alias{get_annotation_completeness}
\title{get_annotation_completeness}
\usage{
get_annotation_completeness(
  package,
  fields = c("lifeStage", "sex"),
  mode = c("any", "all"),
  species = "all",
  by = "deploymentID"
)
}
\arguments{
\item{package}{Camera trap data package object (as returned by \code{read_camtrap_dp()}).}

\item{fields}{Character vector of column names in \code{package$data$observations} to evaluate.
Defaults to \code{c("lifeStage", "sex")}.}

\item{mode}{Character string (\code{"any"} or \code{"all"}). \code{"any"} flags observations where at least
one field is annotated; \code{"all"} requires all specified fields to be non-NA. Defaults to \code{"any"}.}

\item{species}{Scientific or vernacular species name(s) to filter on. Defaults to \code{"all"}.}

\item{by}{Character vector of column name(s) in \code{package$data$deployments} to group by
(e.g., \code{"deploymentID"}, \code{"locationName"}). Use \code{"all"} or \code{NULL} to screen the entire dataset. Defaults to \code{"deploymentID"}.}
}
\value{
A tibble detailing observation counts, percentage annotated, first and last
annotated timestamps, and total annotated duration (in days) per group.
}
\description{
Screen Data Package for Demographic Annotation Coverage.
Identifies time periods and deployment windows where demographic attributes
(\code{lifeStage}, \code{sex}, or custom fields) are actively annotated (non-NA).
}
\examples{
\dontrun{
# Data paths
zipfile <- "../fis-grofwild/Projecten/Drongengoed/Input/Agouti/drongengoed.zip"
exdir <- "../fis-grofwild/Projecten/Drongengoed/Input/Agouti/filesDRG"

# Download and unzip files
download_gdrive_if_missing("1F7mCj-Wm0Ggq1evz1HusxZBgX-1ttx-S", destfile = zipfile)
unzip(zipfile, exdir = exdir)

# Import as Camtrap DP
drg <- camtraptor::read_camtrap_dp(file.path(exdir, "datapackage.json"))

# Check deployments where EITHER lifeStage OR sex is annotated
drg$data$deployments <- drg$data$deployments \%>\%
mutate(year = year(start), month = month(start),
       year_month = floor_date(start, unit = "month"))

coverage_dep <- get_annotation_completeness(
package = drg,
fields = c("lifeStage", "sex"),
mode = "any",
species = "Capreolus capreolus",
by = "year_month"
)
}

}
\author{
Martijn Bollen
}
