% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/generate_colored_pdf.R
\name{generate_colored_pdf}
\alias{generate_colored_pdf}
\title{Generate a Colored PDF from a Line Art Image}
\usage{
generate_colored_pdf(input = NULL, output, color = "#c04384", density = 300)
}
\arguments{
\item{input}{Optional, the file path to the input line art image (e.g., PNG, JPEG).}

\item{output}{The file path where the output PDF should be saved.}

\item{color}{A character string specifying the target color in a format recognized by \code{magick} (e.g., "red", "#FF0000", "rgb(255,0,0)"). The default color is INBO pink ("#c04384").}

\item{density}{An integer specifying the resolution (DPI) for the output PDF. The default is 300, which is suitable for high-quality printing.}
}
\value{
A PDF file saved at the specified output path, containing the colored line art.
}
\description{
This function takes a line art image (like a logo), applies a specified color to the lines, and saves it as a high-resolution PDF suitable for printing. It uses the \code{magick} package to handle image processing.
}
\details{
The function performs the following steps:
0. If the \code{input} parameter is not provided, it prompts the user to select an image file using a file dialog.
\enumerate{
\item Reads the input image using \code{magick::image_read()}.
\item Optionally upscales the image if its width is less than 3000 pixels to ensure better print quality.
\item Creates a mask by converting the image to grayscale, inverting it, and applying a level adjustment to enhance contrast.
\item Creates a blank canvas filled with the specified target color.
\item Applies the mask to the color canvas using the "CopyOpacity" operator, which makes the lines visible in the target color while keeping the background transparent.
\item Optionally flattens the image over a white background to ensure it prints correctly, especially if the PDF viewer does not handle transparency well.
\item Converts the final image to the CMYK colorspace, which is standard for printing.
\item Saves the final image as a PDF with the specified density.
}
}
\examples{
\dontrun{
input_file  <- "image.jpeg"      # Change to your file name
output_file <- "output_cmyk.pdf" # Output file name

generate_colored_pdf(input_file, output_file)
}

}
\author{
Sander Devisscher
}
